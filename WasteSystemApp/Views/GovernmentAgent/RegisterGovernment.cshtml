@model Application.DTOs.GovernmentAgentRequestModel

@{
    ViewData["Title"] = "RegisterGovernment";
}

<style>
    /* Global Styles */
    body {
        background-color: #fff9e6; /* Soft yellow background */
        font-family: 'Arial', sans-serif;
        margin: 0;
        padding: 0;
        color: #333;
    }

    /* Container Styling */
    .row {
        max-width: 600px;
        margin: 50px auto;
        padding: 30px;
        background-color: #fff;
        border-radius: 10px;
        border: 2px solid #ffd700;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.15);
    }

    /* Form Group */
    .form-group {
        margin-bottom: 20px;
    }

    /* Label Styling */
    .control-label {
        font-weight: bold;
        display: block;
        margin-bottom: 8px;
        color: #444;
    }

    /* Input Field */
    .form-control {
        border: 1px solid #333;
        border-radius: 5px;
        padding: 12px;
        width: 100%;
        box-sizing: border-box;
        background-color: #fdfbe4;
        color: #000;
    }

        .form-control:focus {
            border-color: #ffd700;
            box-shadow: 0 0 10px rgba(255, 215, 0, 0.8);
            outline: none;
        }

    /* Button */
    .btn {
        background-color: #ffd700;
        color: #000;
        padding: 12px 20px;
        border: none;
        border-radius: 5px;
        font-weight: bold;
        cursor: pointer;
        transition: background-color 0.3s ease;
        width: 100%; /* Full-width button for better layout */
    }

        .btn:hover {
            background-color: #e6c200;
            color: #fff;
        }

    /* Validation Message */
    .text-danger {
        font-size: 0.9em;
        color: #d9534f;
    }

    /* Link Styling */
    a {
        color: #ffd700;
        text-decoration: none;
        font-weight: bold;
    }

        a:hover {
            text-decoration: underline;
            color: #e6c200;
        }

    /* Heading Styling */
    h1 {
        text-align: center;
        color: #333;
        font-size: 2.5em;
        margin-bottom: 30px;
        text-transform: uppercase;
        letter-spacing: 2px;
    }

    /* HR Styling */
    hr {
        border: 1px solid #ffd700;
        margin: 20px 0;
    }

    .btn-link {
        display: inline-block;
        background-color: #ffd700; /* Gold background */
        color: #000; /* Black text */
        padding: 12px 20px; /* Padding for the button */
        border: none; /* Remove border */
        border-radius: 5px; /* Rounded corners */
        text-align: center; /* Center text */
        text-decoration: none; /* Remove underline */
        font-weight: bold; /* Bold text */
        cursor: pointer; /* Pointer cursor on hover */
        transition: background-color 0.3s ease; /* Smooth hover transition */
    }

        .btn-link:hover {
            background-color: #e6c200; /* Darker gold on hover */
            color: #fff; /* White text on hover */
</style>

<h1>Register Government</h1>
<hr />
<div class="row">
    <form asp-action="RegisterGovernment">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>

        <!-- Email Field -->
        <div class="form-group">
            <label asp-for="Email" class="control-label">Email Address</label>
            <input asp-for="Email" class="form-control" placeholder="Enter your email" />
            <span asp-validation-for="Email" class="text-danger"></span>
        </div>

        <!-- Password Field -->
        <div class="form-group">
            <label asp-for="Password" class="control-label">Password</label>
            <input asp-for="Password" type="password" class="form-control" placeholder="Enter your password" />
            <span asp-validation-for="Password" class="text-danger"></span>
        </div>

        <!-- Sector Name Field -->
        <div class="form-group">
            <label asp-for="SectorName" class="control-label">Sector Name</label>
            <input asp-for="SectorName" class="form-control" placeholder="Enter sector name" />
            <span asp-validation-for="SectorName" class="text-danger"></span>
        </div>

        <!-- State Name Field -->
        <div class="form-group">
            <label asp-for="StateName" class="control-label">State Name</label>
            <input asp-for="StateName" class="form-control" placeholder="Enter state name" />
            <span asp-validation-for="StateName" class="text-danger"></span>
        </div>

        <!-- Submit Button -->
        <div class="form-group">
            <input type="submit" value="Create" class="btn" />
        </div>
    </form>
</div>

<div style="text-align: center; margin-top: 20px;">
    <a asp-controller="Login" asp-action="Login" class="btn-link">Back to Login</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
